interface:
  display_name: "Execute Plan"
  short_description: "Execute a checked-in plan one phase at a time"
  default_prompt: "Use $execute-plan to implement a checked-in plan phase by phase, audit each phase, and commit per phase; push once at the end (monitor CI only if workflows exist)."
policy:
  allow_implicit_invocation: false
