# Documentation References

Wordbreak has no separate code-documentation guide yet. Prefer:

- `README.md` / `methodology.md` / `curriculum.md` for domain truth
- Short file/section comments in `wordbreak_v2.html` only for non-obvious
  constraints, state lifecycle, and cross-cutting contracts
- Do not invent a guide hierarchy; if durable guidance is needed, add a short
  note under `docs/notes/` or extend the product docs
