interface:
  display_name: "Create Plan"
  short_description: "Create implementation plans from repo context"
  default_prompt: "Use $create-plan to write an implementation plan under docs/plans for this request."
policy:
  allow_implicit_invocation: false
